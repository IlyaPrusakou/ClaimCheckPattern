projection implementation in class ZBP_C_PRU_STOCK_REQST unique;
strict ( 2 );
use draft;
define behavior for ZC_PRU_STOCK_REQST alias ZcPruStockReqst

{
  use create;
  use update;
  use delete;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;

}